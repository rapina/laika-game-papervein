# GDD — 종이맥 / Paper Vein

## 콘셉트

- 형식: 순서와 누적 상태가 남는 한 손 건설 게임
- 질문: 벌어지는 종이의 모든 틈을 한 가닥의 섬유로 어떤 순서로 꿰매야 하는가?
- 감각: 한 번 당긴 결과가 종이 전체로 번지며 다음 선택을 바꾸고, 작은 봉합이 마지막에 단단한 한 장을 만든다.
- 세션: 첫 플레이 90–105초, 숙련 플레이 60–80초
- 세계: 햇빛이 드는 낮은 수선틀 위에서 젖은 종이 한 장이 천천히 마르며 벌어진다. 종이, 한 가닥의 섬유, 구멍과 틈만 보인다.
- 대표 색: 주홍 `#E64B32`. 실과 핵심 상태에만 쓴다.

## 한 판

1. 타이틀 키 이미지 위에서 시작한다. 시작 입력 전에는 소리가 나지 않는다.
2. 종이에는 눌린 구멍 8개와 그 사이의 틈 7개가 있다. 실 끝은 첫 구멍에 묶여 있다.
3. 느슨한 실 끝을 집어 아직 비어 있는 구멍까지 드래그해 놓는다. 키보드는 방향키로 구멍을 고르고 Space로 집기/놓기를 한다.
4. 봉합 뒤에는 종이·풀·섬유가 9초 동안 안정화된다. 주홍 실 끝이 천천히 느슨해지고 구멍의 원형 진행이 차며 주름이 이완되는 동안 다음 봉합을 받지 않는다. 이때 다시 당기면 실과 종이가 반동하고 `아직 팽팽하다 / STILL TAUT`를 보여 준다.
5. 안정화가 끝나면 가장 장력이 높은 미봉합 틈의 굵은 주름이 다음 구멍을 또렷하게 가리킨다. 그 뒤에 느슨한 실 끝을 잡아 다음 봉합을 고른다.
6. 봉합은 대상 틈을 닫고 경로 안의 틈도 조금 좁히지만, 거리에 따라 실을 더 쓰고 주변 장력을 더 높인다.
7. 종이는 시작 뒤 고정 스텝으로 계속 벌어진다. 틈 하나가 한계를 넘으면 파열되고, 갈라진 외곽선과 짧은 진동 무늬로 표시된다.
8. 7번째 봉합의 안정화까지 끝나면 종이가 수선틀에서 들리고, 역광으로 종이결과 주홍 봉합선이 하나의 잎맥처럼 드러난다.
9. 세 번째 파열 또는 실 소진이면 실패한다. 결과는 700ms 동안 입력에서 보호된 뒤 화면 탭/Space로 다시 시작한다.

## 순수 규칙

- 논리 스텝: `1/60초`, 입력을 시간 진행 전에 소비한다.
- 시드: `mulberry32` 계열 32비트 시드. 시드는 구멍 위치의 미세 오프셋과 초기 틈 차이만 정하고 같은 입력은 같은 결과를 만든다.
- 구멍: `0..7`. 실 끝은 0에서 시작하며, `1..7`은 각 틈의 봉합 구멍이다.
- 초기 틈 벌어짐: `0.690..0.748`. 화면에서 틈의 폭과 어두운 내부 면적으로 읽힌다.
- 재벌어짐: 미봉합 틈은 초당 `0.0155 + 장력 × 0.0035`, 봉합 틈은 초당 `0.0012` 벌어진다.
- 파열: 벌어짐이 `1.0`에 도달하면 파열 1회가 나고 해당 틈 벌어짐은 `0.62`, 장력은 `0.38`로 돌아간다. 종이 전체가 반동해 다른 미봉합 틈은 `0.06` 좁아지고 4초 동안 추가 자연 파열이 나지 않는다. 위험 봉합의 즉시 파열은 이 보호를 받지 않는다. 세 번째 파열에서 종료한다.
- 실: 시작 `100`.
- 안정화: 유효 봉합마다 `9초`. `step`이 남은 시간을 매 틱 줄이고 `0`일 때만 다음 봉합을 받는다. 7번째 봉합도 안정화가 끝나야 성공하므로, 즉시 첫 봉합을 시작한 숙련 경로의 규칙상 최단 완주는 약 `63초`다.
- 거리 `d`: 현재 실 끝 구멍과 대상 구멍의 인덱스 거리 `abs(from - to)`.
- 봉합 비용: `5 + 2.4d + 4 × 대상 장력 + 1.5 × 이미 파열된 대상 횟수`.
- 당김:
  - 대상 틈의 벌어짐은 `0.08`로 닫힌다.
  - 현재 끝과 대상 사이에 있는 미봉합 틈은 각각 `max(0.08, 벌어짐 - (0.16 + 0.025d))`가 된다.
  - 대상 주변 미봉합 틈은 거리별로 장력 `0.065d / (1 + 이웃 거리)`를 얻는다.
  - 대상 틈은 `0.08 + 0.035d` 장력을 얻는다.
  - 모든 장력은 매초 `0.006`씩 마른다.
- 위험 봉합: drag 중 `대상 현재 장력 + 0.08 + 0.1d`로 놓은 직후 장력을 미리 계산한다. 이 값이 `0.72` 이상이면 놓기 전부터 대상 틈의 갈라진 이중 외곽선과 진동 무늬로 예고하며, 놓은 직후 실제 같은 값으로 즉시 파열한다.
- 실 소진: 봉합 비용을 낼 실이 없으면 즉시 실패한다.
- 성공: 서로 다른 7개 대상 구멍을 모두 사용하고 마지막 9초 안정화가 끝난 순간.
- 점수: `닫힌 틈 × 10,000 + 남은 실 × 100 - 파열 × 1,000 - 경과 초`. 진행이 가장 높은 자릿수다.
- 결과 교정: 가장 높은 장력이 남은 틈의 방향과, 다음 판에서 먼저 당길 구체적인 구멍 하나를 ko/en으로 보여 준다.

## 화면에 보이는 판정 정보

- 벌어짐: 찢어진 가장자리 사이의 실제 간격.
- 장력: 구멍에서 뻗는 짧은 주름의 길이와 밀도. 현재 가장 팽팽한 주름 하나는 다음 방향으로 굵어진다.
- 실 잔량: 화면 위쪽에 남은 한 가닥의 길이로 표시하며 숫자도 함께 둔다. drag preview는 내부 비용 단위 대신 정확한 비용식을 적용한 `봉합 뒤 남을 실` 숫자를 보여 준다.
- 파열 예산: 종이 가장자리의 3개 절취 흔적. 파열 때 하나가 실제로 갈라진다.
- 사용한 구멍: 주홍 실이 통과하며 눌린 고리가 닫힌다.
- 가이드 3답: `실 끝을 잡아 빈 구멍에 놓기` / `실이 느슨해진 뒤 굵은 주름부터` / `7개 봉합 · 파열 3회 전`.
- 가이드는 성공 2회까지 보이고, 사라진 뒤 2회 연속 파열이면 돌아온다.
- 게임 오버 2답: 성공/실패 제목으로 판 종료를 알리고 `화면을 탭해 다시 수선 / Tap to mend again`을 표시한다.

## 입력과 수명주기

- 포인터: 실 끝의 54px 집기 영역에서 `pointerdown`, 이동 중 정확한 비용식으로 계산한 봉합 뒤 실 잔량·predicted tension 위험 외곽선·당김 경로를 실시간 표시, 빈 구멍 54px 영역에서 `pointerup`. 안정화 중에도 같은 자연 제스처를 받고, 놓을 때 반동과 상태 피드백을 준 뒤 capture를 정상 해제해 readiness 뒤 다시 시도할 수 있다.
- 키보드: `window`의 좌우/상하 방향키로 빈 구멍 순환, Space로 집기/놓기, M으로 음소거.
- 한 프레임 최대 5스텝으로 `dt`를 제한하고 나머지는 버려 복귀 점프를 막는다.
- 호스트 pause, `visibilitychange`, 음소거, 재시작은 규칙 시계와 모든 오디오 채널에 함께 적용한다.
- 독립 실행은 ko/en 전환을 제공하고 전환 중 규칙 상태를 보존한다. 포털에서는 호스트 locale을 따른다.

## 아트와 반응

- 손으로 뜬 두꺼운 종이와 젖은 닥섬유의 생성 원재료 PNG를 2D 종이 메시, 찢어진 가장자리 마스크, 실 곡선과 합성한다.
- 봉합 직후 대상 틈의 양쪽 가장자리가 실제로 안쪽으로 접히고, 주홍 실은 살짝 지나쳤다가 팽팽하게 돌아온다. 주변 주름은 당김 방향으로 재배열된다.
- 파열 직후 틈이 벌어지고 종이 조각 가장자리가 비대칭으로 떨린다. 색만 바꾸지 않는다.
- 결과 장면은 종이 전체가 들리며 배경광이 종이결 사이를 통과한다. 성공 결과 정보는 아래쪽의 작은 반투명 띠에 두어 누적 잎맥과 역광을 가리지 않는다. 실패 정보판은 원인 판독을 위해 기존 대비를 유지한다.
- 효과음은 Web Audio로 젖은 섬유의 낮은 마찰음, 봉합의 짧은 장력음, 파열의 마른 종이음을 합성한다. 반복 배경음과 장식 파티클은 없다.

## 플레이 가능성 검증

- 프로필: `construction`.
- 실제 `step`을 매 틱 호출한다.
- 서로 다른 세 봉합 순서가 최종 장력, 실 잔량, 파열/완주 결과를 달리해야 한다.
- 최소 한 경로가 실제 `stabilizationRemaining` readiness를 따라 행동하며 60–80초 안에 완주해야 한다. 고정 9초 sleep이나 닫힌 시간식으로 대체하지 않는다.
- 무입력은 20초 안에 첫 파열, 세션 상한 안에 세 번째 파열이 나야 한다.
- 깊이 A/B: 같은 시드와 행동 간격에서 화면의 굵은 장력 주름을 우선하는 정책이 이를 무시하는 순서보다 완주율 또는 남은 실/파열 복합 결과에서 우위여야 한다.

## 제외

바늘 직접 조작, 자유 곡선 자수, 인벤토리, 실 색 수집, 업그레이드, 콤보 배지, 장식 파티클, 물리 기반 실 엉킴, 색만으로 전달하는 장력, 정밀 타이밍 보너스, 게임 도중 긴 문장.
